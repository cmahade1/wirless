Readme:
This is one of the part of the project, the web development part.
It is basically developed through node js in backend and javascript in front end.
After this have a look at the android project part readme file and then cumulatively the system can
be used.
Note: Be aware that only after this part the mobile app can be up and running.


Install node (javascript):
sudo apt-get update
sudo apt-get install nodejs

Install npm package manager:
sudo apt-get install npm

RUN:

To install project dependent node modules-
npm install

Running the local host and exposing it to public ip (The shell script)-
node index.js 8080 & ./ngrok http 8080

Copy and paste the ip displayed in terminal into the android studio manager. Follow up the readme in android project part.

